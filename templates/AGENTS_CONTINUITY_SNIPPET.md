Continuity rule:

- For non-trivial or long-running tasks, read `<workspace>/codex-continuity/GLOBAL_STATE.md` before deep work.
- If losing chat context would be expensive, create or update a task journal under `<workspace>/codex-continuity/journals`.
- Update the relevant journal before risky operations, long remote work, scope switches, and final response.
- When resuming a broken or compact-failed thread, use `<workspace>/codex-continuity/session-index.md` or a file under `<workspace>/codex-continuity/rescues` as the recovery entrypoint.
- Do not publish generated continuity outputs without reviewing and redacting private paths, hosts, commands, tokens, customer data, or project-specific strategy.
